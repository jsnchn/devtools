# Syncthing ignore rules for secrets directory
# Only these files are synced:

# Always sync these:
!.stignore
!.pushover.env
!openrouter-api-key
!context7-api-key
!opencode-auth.json

# Ignore everything else (per-machine secrets)
*
!*.stignore